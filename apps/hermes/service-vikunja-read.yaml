apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app.kubernetes.io/instance: hermes
    app.kubernetes.io/name: hermes
  name: hermes-vikunja-read
  namespace: hermes
spec:
  type: ClusterIP
  # The facade has its own health probe; do not couple read access to unrelated sidecars.
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: hermes
  ports:
    - name: read
      port: 8793
      targetPort: vikunja-read
      protocol: TCP
