apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: volsync-canary-data
  namespace: volsync-canary
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-rbd
