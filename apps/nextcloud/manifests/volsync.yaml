apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: nextcloud-data
  namespace: nextcloud
spec:
  sourcePVC: nextcloud-nextcloud
  trigger:
    schedule: "30 */6 * * *"
  restic:
    repository: volsync-restic-nextcloud-data
    copyMethod: Snapshot
    storageClassName: ceph-cephfs
    volumeSnapshotClassName: csi-cephfsplugin-snapclass-v2
    cacheCapacity: 1Gi
    cacheStorageClassName: ceph-cephfs
    pruneIntervalDays: 7
    retain:
      daily: 7
      weekly: 5
      monthly: 6
