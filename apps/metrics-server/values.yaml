apiService:
  create: true
  insecureSkipTLSVerify: true

args:
  - --kubelet-insecure-tls

serviceMonitor:
  enabled: true
  additionalLabels:
    release: kube-prometheus-stack

resources:
  requests:
    cpu: 100m
    memory: 200Mi
  limits:
    cpu: 300m
    memory: 400Mi
