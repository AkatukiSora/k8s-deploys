apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: immich-postgres-barman-store
  namespace: immich-postgres
spec:
  configuration:
    destinationPath: s3://soras-home-k8s-backup/cnpg/immich-postgres
    endpointURL: https://s3.us-west-004.backblazeb2.com
    s3Credentials:
      accessKeyId:
        name: immich-postgres-barman-s3
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: immich-postgres-barman-s3
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
      maxParallel: 2
    data:
      compression: gzip
      jobs: 2
