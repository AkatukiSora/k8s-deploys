# Kubernetes Permissions

Place Kubernetes RBAC manifests here when tenant, platform, or shared permission objects need to be managed separately from application installs.

Examples:

- shared ClusterRoles for tenant users
- platform-only ClusterRoleBindings
- aggregated roles for namespaced CRDs
