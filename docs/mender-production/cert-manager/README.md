# Certificate scope

No cert-manager or origin TLS resource is part of the selected Mender design.
Cloudflare terminates TLS for `mender.sora-lab.dev` and the Tunnel reaches the
Mender gateway over HTTP. This directory is retained only as historical
planning context and is not referenced by any Mender Application.
