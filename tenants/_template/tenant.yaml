apiVersion: capsule.clastix.io/v1beta2
kind: Tenant
metadata:
  name: __TENANT__
spec:
  additionalRoleBindings:
    - clusterRoleName: edit
      subjects:
        - apiGroup: rbac.authorization.k8s.io
          kind: Group
          name: oidc:app:k8s:tenant:__TENANT__:user
